//Este header va a ser generado de manera automatica al compilar y "terminar la compra" (funcion del programa). //

//En este header se dara el TICKET de manera automatica y se ira actualizando por cada ejecucion del programa (cada vez que el ususario lo compile y termine compra tras compra)//
